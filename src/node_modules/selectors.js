import * as R from 'ramda'

export const getPostById = (state, id) => R.prop(id, state.posts)
export const getPosts = state => {
const posts = R.map(id => getPostById(state, id), state.postsPage.ids)
  return posts
}